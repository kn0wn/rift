"use client";

// This component is no longer needed since we removed authentication
// Keeping it as a placeholder in case we need to add cleanup logic later
export default function AuthCleanup() {
  return null;
}
