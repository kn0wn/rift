// Removed Clerk authentication middleware
// The app now works without authentication requirements

export function middleware() {
  // No middleware needed since we removed authentication
}

export const config = {
  matcher: [],
};
