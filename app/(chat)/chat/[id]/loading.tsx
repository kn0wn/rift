export default function Loading() {
  // Intentionally blank to avoid layout shifts; the static shell (sidebar header + prompt)
  // is rendered by the page itself and becomes interactive after hydration.
  return null;
}


